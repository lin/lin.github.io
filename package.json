{
  "name": "lin-github-io",
  "version": "1.0.0",
  "description": "Hugo site with React widgets",
  "scripts": {
    "build:widgets": "node scripts/build-widgets.js",
    "watch:widgets": "node scripts/build-widgets.js --watch",
    "dev": "npm run watch:widgets & hugo server -D",
    "build": "npm run build:widgets && hugo"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
